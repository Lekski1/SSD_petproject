events {
    worker_connections 1024;
}

http {
    upstream vulnerable-app {
        server vulnerable-app1:8000;
        server vulnerable-app2:8000;
        server vulnerable-app3:8000;
    }

    server {
        listen 8000;

        location / {
            proxy_pass http://vulnerable-app;
        }
    }
}
